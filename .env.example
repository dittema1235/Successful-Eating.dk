# Never put API keys in PUBLIC_ variables.
# Optional: overrides the default Onlinebooq booking link for the 50-minute consultation
# (https://dittemunchandersn.onlinebooq.dk/, set in src/data/site.ts).
PUBLIC_CONSULTATION_BOOKING_URL=
