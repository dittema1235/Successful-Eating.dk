# Never put API keys in PUBLIC_ variables.
# Optional: verified HTTPS booking/payment link for the 50-minute consultation at 645 DKK.
# Without this, visitors can explicitly request an appointment by email.
PUBLIC_CONSULTATION_BOOKING_URL=
